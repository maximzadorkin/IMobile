const protocol = 'http://'
const domain = '192.168.137.40'
const port = ':3030'
const url = `${protocol}${domain}${port}`

const LOG_IN_API = `${url}/api/login`

const Api = {
    LOG_IN_API
}

export default Api